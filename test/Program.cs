//using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace test
{
    class Program
    {

        public static bool rr(int a, int b, int c)
        {
            return ((a == b) || (a == c) || (b == c));
        }

        public static bool rs(int a, int b, int c)
        {
            return ((a == b) && (a == c) && (b == c));
        }

        public static bool pu(int a, int b, int c)
        {
            return ((a * a == b * b + c * c) || (b * b == c * c + a * a) || (c * c == a * a + b * b));
        }

        static void Main(string[] args)
        {
            int x = 1;

            string text = "Ahoj";
            text.

            System.Console.WriteLine("abc" + 1);
            System.Console.Read();
        }
    }
}
